apiVersion: v1
kind: Service
metadata:
  name: manufacturing-service
  namespace: dev
spec:
  type: ClusterIP
  selector:
    app: manufacturing-service
  ports:
    - port: 8085
      targetPort: 8085
      protocol: TCP
