apiVersion: v1
kind: Service
metadata:
  name: drug-catalog-service
  namespace: dev
spec:
  type: ClusterIP
  selector:
    app: drug-catalog-service
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
