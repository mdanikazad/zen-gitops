apiVersion: v1
kind: Service
metadata:
  name: supplier-service
  namespace: dev
spec:
  type: ClusterIP
  selector:
    app: supplier-service
  ports:
    - port: 8084
      targetPort: 8084
      protocol: TCP
